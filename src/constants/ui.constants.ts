export const DEFAULT_PAGE = 1;
export const DEFAULT_LIMIT = 10;
/** DummyJSON returns the full collection when limit is 0. */
export const CATALOG_LIMIT = 0;
export const DEFAULT_PAGE_OPTIONS = [10, 25, 50, 100];
export const HEADER_HEIGHT = 64;
export const FILTER_SIDEBAR_WIDTH = 280;
export const FILTER_VISIBLE_COUNT = 10;
